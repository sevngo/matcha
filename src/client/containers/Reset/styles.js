import makeStyles from '@mui/styles/makeStyles';

export default makeStyles((theme) => ({
  p3: {
    padding: theme.spacing(3),
  },
  paper: {
    padding: theme.spacing(3),
    maxWidth: '30em',
  },
}));
